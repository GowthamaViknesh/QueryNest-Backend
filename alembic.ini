# Alembic = database migrations for the API's own tables (like TypeORM migrations).
#   uv run migrate                                 apply all migrations
#   uv run alembic revision --autogenerate -m "x"  create a migration from appdb.py changes
[alembic]
script_location = %(here)s/alembic
prepend_sys_path = src

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
